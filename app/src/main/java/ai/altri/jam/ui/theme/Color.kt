package ai.altri.jam.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors (brown theme)
val Primary = Color(0xFF8B4513)      // Saddle brown for app bar
val PrimaryLight = Color(0xFFA0522D)  // Lighter brown
val PrimaryDark = Color(0xFF6B3811)   // Darker brown

// Secondary colors (bot messages)
val Secondary = Color(0xFFF5DEB3)     // Wheat color for bot messages
val SecondaryLight = Color(0xFFF7E7C3) // Lighter wheat
val SecondaryDark = Color(0xFFDCC69F)  // Darker wheat

// Tertiary colors (user messages)
val Tertiary = Color(0xFF8B4513)      // Saddle brown for user messages
val TertiaryLight = Color(0xFFA0522D)  // Lighter brown
val TertiaryDark = Color(0xFF6B3811)   // Darker brown

// Base colors
val Black = Color(0xFF000000)
val White = Color(0xFFFFFFFF)

// Background colors
val Background = Color(0xFFFFFFFF)     // Pure white background
